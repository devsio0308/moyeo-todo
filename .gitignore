# Node / Electron
node_modules/
electron-app/out/
electron-app/dist/
electron-app/resources/engine/

# Python
__pycache__/
*.pyc
python-engine/build/
python-engine/dist/
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db
*.log

# Firebase 비밀키 예방 조치 — 서비스 계정 키는 절대 커밋 금지
*firebase-adminsdk*.json
serviceAccount*.json
service-account*.json
.firebaserc

# 로컬 환경변수 (프로젝트 ID 등) — .env.example만 커밋
electron-app/.env
electron-app/.env.*
!electron-app/.env.example
web-overlay/.env
web-overlay/.env.*
!web-overlay/.env.example

# web-overlay 빌드 산출물
web-overlay/node_modules/
web-overlay/dist/
web-overlay/.firebase/
web-overlay/*.tsbuildinfo
